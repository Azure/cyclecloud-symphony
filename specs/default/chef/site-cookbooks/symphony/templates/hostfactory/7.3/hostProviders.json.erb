{
    "version": 2,
    "providers":[
       {
            "name": "azurecc",
            "enabled": 1,
            "plugin": "azurecc", 
            "confPath": "${HF_CONFDIR}/providers/azurecc/conf/",
            "workPath": "${HF_WORKDIR}/providers/azurecc/",
            "logPath": "${HF_LOGDIR}/"
       }
    ]
}